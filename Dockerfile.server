# Node.js API 服务器
FROM node:20-alpine

WORKDIR /app

# 复制服务器代码和 package.json
COPY server/package.json ./
RUN npm install

COPY server/ ./

# 创建 data 目录
RUN mkdir -p /app/data

# 暴露端口
EXPOSE 3001

# 启动服务器
CMD ["npm", "start"]