version: '3.8'

services:
  nano-banana-supermarket:
    image: lisankai93/nano-banana-supermarket:latest
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
      - API_KEY=sk-BPG9OYmbBPJc3xJX8qpuEoAfvaHhCPwu2j8MX59F4Mg4RcPD
      - API_BASE_URL=https://api.ephone.ai
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  app-data: